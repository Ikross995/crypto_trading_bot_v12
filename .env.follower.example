# ==========================================
# 🔄 COPY TRADER - FOLLOWER MODE
# ==========================================
# Конфигурация для копирования сигналов из Telegram канала
#
# Использование:
#   1. Скопируйте этот файл: cp .env.follower.example .env.follower
#   2. Заполните свои данные
#   3. Запустите: python start_copy_trader.py --mode follower --env .env.follower

# ==========================================
# TELEGRAM НАСТРОЙКИ
# ==========================================

# Токен вашего Telegram бота (получить у @BotFather)
TG_BOT_TOKEN=your_bot_token_here

# Канал/группа откуда копировать сигналы
# Формат: @channel_username или -1001234567890 (для приватных каналов)
TG_SOURCE_CHANNEL=@trading_signals_channel

# ==========================================
# BINANCE API
# ==========================================

# API ключи для Binance Futures
BINANCE_API_KEY=your_binance_api_key
BINANCE_API_SECRET=your_binance_api_secret

# Использовать Testnet (true/false)
# ВАЖНО: Сначала протестируйте на testnet!
TESTNET=true

# Dry Run режим - НЕ выполнять реальные ордера (true/false)
# Полезно для тестирования парсинга сигналов
DRY_RUN=false

# ==========================================
# НАСТРОЙКИ КОПИРОВАНИЯ
# ==========================================

# Множитель размера позиции (0.1 = 10% от размера лидера)
# Если лидер открывает $1000, вы откроете $100 при multiplier=0.1
COPY_POSITION_SIZE_MULTIPLIER=0.5

# Максимальный размер позиции в USDT
COPY_MAX_POSITION_SIZE=100.0

# Минимальный размер позиции в USDT
COPY_MIN_POSITION_SIZE=10.0

# ==========================================
# РИСК-МЕНЕДЖМЕНТ
# ==========================================

# Максимальное плечо для копирования
# Если сигнал содержит leverage выше этого значения, сделка будет пропущена
COPY_MAX_LEVERAGE=10

# Максимальное количество одновременно открытых позиций
COPY_MAX_OPEN_POSITIONS=5

# Дневной лимит убытков в процентах от баланса
# Если дневные убытки превысят этот процент, новые позиции не будут открываться
COPY_DAILY_LOSS_LIMIT_PCT=5.0

# ==========================================
# ФИЛЬТРАЦИЯ СИМВОЛОВ (опционально)
# ==========================================

# Список разрешённых символов для копирования (через запятую)
# Оставьте пустым для копирования всех символов
# Пример: BTCUSDT,ETHUSDT,BNBUSDT
COPY_ALLOWED_SYMBOLS=BTCUSDT,ETHUSDT,BNBUSDT,SOLUSDT,ADAUSDT,XRPUSDT

# ==========================================
# ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ
# ==========================================

# Уровень логирования (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Директория для логов
LOG_DIR=logs

# ==========================================
# ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ
# ==========================================

# Тестирование на testnet:
#   python start_copy_trader.py --mode follower --env .env.follower --testnet

# Реальная торговля:
#   1. Установите TESTNET=false
#   2. python start_copy_trader.py --mode follower --env .env.follower

# Только парсинг (без выполнения ордеров):
#   python start_copy_trader.py --mode follower --env .env.follower --dry-run
