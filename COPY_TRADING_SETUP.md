# Copy Trading Setup Guide

Полное руководство по настройке системы копитрейдинга для crypto_trading_bot_v12.

## 📋 Содержание

1. [Обзор системы](#обзор-системы)
2. [Архитектура](#архитектура)
3. [Быстрый старт](#быстрый-старт)
4. [Настройка копировщиков](#настройка-копировщиков)
5. [Система монетизации](#система-монетизации)
6. [Безопасность](#безопасность)
7. [API Reference](#api-reference)

---

## 🎯 Обзор системы

Система копитрейдинга позволяет:

- **Для мастер-трейдеров**: Делиться своими торговыми стратегиями и зарабатывать на успешных копировщиках
- **Для копировщиков**: Автоматически копировать сделки успешных трейдеров с настраиваемыми параметрами

### Ключевые возможности

✅ **Real-time копирование** - мгновенная репликация сделок через WebSocket
✅ **Гибкие настройки** - 3 режима расчета позиций (fixed_ratio, fixed_amount, percentage)
✅ **Риск-менеджмент** - автоматический контроль рисков для каждого копировщика
✅ **Прозрачная монетизация** - справедливое распределение прибыли по модели "High Water Mark"
✅ **Масштабируемость** - поддержка 1000+ копировщиков на одного мастера

---

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────────────┐
│              Trading Bot (Master Trader)                │
│                                                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │          TradingBotAdapter                       │  │
│  │  • Перехват сигналов от основного бота          │  │
│  │  • Нормализация формата                         │  │
│  └──────────────┬───────────────────────────────────┘  │
└─────────────────┼───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│            CopyTradingEngine (Core)                     │
│                                                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Signal Queue                                    │  │
│  │  • Асинхронная обработка сигналов               │  │
│  │  • Параллельное распределение                   │  │
│  └──────────────┬───────────────────────────────────┘  │
│                 │                                        │
│  ┌──────────────▼───────────────────────────────────┐  │
│  │  Position Calculator                             │  │
│  │  • fixed_ratio: соотношение к мастеру           │  │
│  │  • fixed_amount: фиксированная сумма USDT       │  │
│  │  • percentage: % от баланса                     │  │
│  └──────────────┬───────────────────────────────────┘  │
│                 │                                        │
│  ┌──────────────▼───────────────────────────────────┐  │
│  │  Risk Manager                                    │  │
│  │  • Проверка max_position_size                   │  │
│  │  • Проверка max_open_positions                  │  │
│  │  • Проверка max_daily_loss                      │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────┼───────────────────────────────────────┘
                  │
        ┌─────────┴─────────┐
        │                   │
        ▼                   ▼
┌──────────────┐   ┌──────────────┐
│  Copier 1    │   │  Copier N    │
│ (Sub-account)│   │ (Sub-account)│
└──────────────┘   └──────────────┘
```

---

## 🚀 Быстрый старт

### Шаг 1: Установка зависимостей

```bash
pip install python-binance aiogram aiohttp
```

### Шаг 2: Настройка мастер-трейдера

```python
from infra.copy_trading import CopyTradingEngine, CopierAccount
from infra.trading_bot_adapter import TradingBotAdapter
from decimal import Decimal

# 1. Создаем движок копитрейдинга
copy_engine = CopyTradingEngine()

# 2. Подключаем адаптер к существующему боту
adapter = TradingBotAdapter(your_existing_bot)
await adapter.connect_to_existing_bot()

# 3. Связываем адаптер с движком
adapter.copy_trading_engine = copy_engine

# 4. Запускаем обработку
await copy_engine.start_polling()
```

### Шаг 3: Добавление копировщиков

```python
# Создаем аккаунт копировщика
copier = CopierAccount(
    user_id=1,
    email="copier@example.com",
    api_key="binance_api_key",
    api_secret="binance_api_secret",

    # Режим копирования: фиксированное соотношение к мастеру
    position_mode='fixed_ratio',
    copy_ratio=Decimal('0.1'),  # 10% от размера сделки мастера

    # Риск-менеджмент
    max_position_size=Decimal('1000'),  # Максимум $1000 на позицию
    max_open_positions=10,  # Максимум 10 открытых позиций
    max_daily_loss=Decimal('-500'),  # Дневной лимит убытков -$500
)

# Настройки копировщика
copier.settings = {
    'copy_ratio': 0.1,
    'max_position_size': 1000,
    'max_open_positions': 10,
    'max_daily_loss': -500,
    'slippage': 0.001  # 0.1% проскальзывание
}

# Добавляем в систему
copy_engine.add_copier(copier)
```

---

## ⚙️ Настройка копировщиков

### Режимы расчета позиций

#### 1. Fixed Ratio (Фиксированное соотношение)

Копирует сделки с фиксированным коэффициентом к мастеру.

```python
copier.position_mode = 'fixed_ratio'
copier.copy_ratio = Decimal('0.1')  # 10% от размера мастера

# Пример:
# Мастер открывает 1.0 BTC
# Копировщик откроет 0.1 BTC (10%)
```

**Подходит для**: пропорционального копирования с меньшим капиталом

#### 2. Fixed Amount (Фиксированная сумма)

Каждая сделка открывается на фиксированную сумму в USDT.

```python
copier.position_mode = 'fixed_amount'
copier.fixed_amount = Decimal('100')  # $100 на каждую сделку

# Пример:
# Независимо от размера сделки мастера,
# копировщик всегда открывает позицию на $100
```

**Подходит для**: равномерного распределения капитала

#### 3. Percentage (Процент от баланса)

Каждая сделка открывается на % от баланса копировщика.

```python
copier.position_mode = 'percentage'
copier.balance_percentage = Decimal('0.05')  # 5% от баланса

# Пример:
# Баланс копировщика: $10,000
# Каждая сделка откроется на $500 (5%)
```

**Подходит для**: динамического управления капиталом

### Риск-менеджмент

```python
copier.settings = {
    # Максимальный размер одной позиции (USDT)
    'max_position_size': 1000,

    # Максимальное количество открытых позиций
    'max_open_positions': 10,

    # Дневной лимит убытков (USDT)
    'max_daily_loss': -500,

    # Проскальзывание для лимитных ордеров
    'slippage': 0.001,  # 0.1%
}
```

### Фильтрация торговых пар

```python
# Копировать только определенные пары
copier.allowed_pairs = ['BTCUSDT', 'ETHUSDT', 'BNBUSDT']

# Копировать все пары (по умолчанию)
copier.allowed_pairs = None
```

---

## 💰 Система монетизации

### Модели монетизации

#### 1. Profit Sharing (Распределение прибыли)

Мастер-трейдер получает % от прибыли копировщиков.

```python
from infra.profit_sharing import ProfitSharingSystem

profit_system = ProfitSharingSystem(db_connection)

# Еженедельный расчет прибыли
settlement = await profit_system.calculate_weekly_settlement(user_id=1)

print(f"Total P&L: ${settlement['total_pnl']:.2f}")
print(f"Profit Share (10%): ${settlement['profit_share']:.2f}")
print(f"Total Payment: ${settlement['total_payment']:.2f}")
```

**Параметры по умолчанию:**
- Доля мастера от прибыли: **10%**
- Доля от комиссий: **10%**
- Расчет: еженедельно

#### 2. High Water Mark (HWM)

Комиссия начисляется только при превышении исторического максимума баланса.

```python
# Расчет комиссии по HWM
hwm_commission = await profit_system.calculate_high_water_mark(user_id=1)

# Логика:
# - Текущий баланс: $1,150
# - Исторический максимум (HWM): $1,000
# - Прибыль выше HWM: $150
# - Комиссия (10%): $15
```

**Преимущества HWM:**
- ✅ Справедливо для копировщиков
- ✅ Мотивирует мастера не допускать просадок
- ✅ Комиссия только за реальную прибыль

### Планы подписок

```python
from infra.profit_sharing import SubscriptionManager

subscription_mgr = SubscriptionManager(db_connection)

# Basic Plan (Бесплатно)
# - До 10 копировщиков
# - Базовая статистика
# - Email уведомления

# Pro Plan ($49.99/месяц)
# - До 100 копировщиков
# - Расширенная статистика
# - Telegram уведомления
# - API доступ

# Enterprise Plan ($299.99/месяц)
# - До 1000 копировщиков
# - Все функции
# - Приоритетная поддержка
# - Кастомная интеграция
```

---

## 🔐 Безопасность

### Хранение API ключей

**НИКОГДА** не храните API ключи в открытом виде!

```python
from cryptography.fernet import Fernet
from cryptography.hazmat.primitives import hashes
from cryptography.hazmat.primitives.kdf.pbkdf2 import PBKDF2
import os
import base64

class SecureKeyStorage:
    """Безопасное хранение API ключей с шифрованием."""

    def __init__(self, master_password: str):
        # Генерация ключа шифрования из мастер-пароля
        kdf = PBKDF2(
            algorithm=hashes.SHA256(),
            length=32,
            salt=os.environ.get('ENCRYPTION_SALT').encode(),
            iterations=100000,
        )
        key = base64.urlsafe_b64encode(kdf.derive(master_password.encode()))
        self.cipher = Fernet(key)

    def encrypt_api_key(self, api_key: str, api_secret: str) -> dict:
        """Шифрование API ключей перед сохранением в БД."""
        return {
            'encrypted_key': self.cipher.encrypt(api_key.encode()).decode(),
            'encrypted_secret': self.cipher.encrypt(api_secret.encode()).decode(),
            'key_hash': hashlib.sha256(api_key.encode()).hexdigest()[:16]
        }

    def decrypt_api_key(self, encrypted_data: dict) -> tuple:
        """Расшифровка API ключей для использования."""
        api_key = self.cipher.decrypt(encrypted_data['encrypted_key'].encode()).decode()
        api_secret = self.cipher.decrypt(encrypted_data['encrypted_secret'].encode()).decode()
        return (api_key, api_secret)
```

### Изоляция ключей

✅ **Используйте отдельные ключи для каждого копировщика**
✅ **Ограничьте права API ключей** (без вывода средств)
✅ **Ротация ключей каждые 90 дней**
✅ **Временные токены для аутентификации**

### Защита от атак

```python
# Rate limiting на все API endpoints
from fastapi import FastAPI, Request
from slowapi import Limiter, _rate_limit_exceeded_handler
from slowapi.util import get_remote_address

limiter = Limiter(key_func=get_remote_address)
app = FastAPI()
app.state.limiter = limiter

@app.post("/api/copy_trade")
@limiter.limit("10/minute")  # Максимум 10 запросов в минуту
async def copy_trade(request: Request):
    pass

# DDoS защита через Cloudflare
# SQL injection защита через параметризованные запросы
# XSS защита через валидацию входных данных
```

---

## 📡 API Reference

### CopyTradingEngine

#### `add_copier(copier: CopierAccount)`

Добавить копировщика в систему.

```python
copier = CopierAccount(...)
copy_engine.add_copier(copier)
```

#### `remove_copier(user_id: int)`

Удалить копировщика из системы.

```python
copy_engine.remove_copier(user_id=1)
```

#### `process_master_trade(trade: Dict[str, Any])`

Обработать сделку от мастер-трейдера.

```python
trade = {
    'symbol': 'BTCUSDT',
    'side': 'BUY',
    'quantity': 1.0,
    'price': 50000.0,
    'type': 'MARKET',
    'time': 1234567890,
    'orderId': 'master_order_123'
}

await copy_engine.process_master_trade(trade)
```

### ProfitSharingSystem

#### `calculate_weekly_settlement(user_id: int)`

Рассчитать еженедельное вознаграждение для копировщика.

```python
settlement = await profit_system.calculate_weekly_settlement(user_id=1)
```

#### `calculate_high_water_mark(user_id: int)`

Рассчитать комиссию по модели High Water Mark.

```python
commission = await profit_system.calculate_high_water_mark(user_id=1)
```

---

## 🎓 Примеры использования

### Полная настройка системы

```python
import asyncio
from infra.copy_trading import CopyTradingEngine, CopierAccount
from infra.trading_bot_adapter import TradingBotAdapter
from infra.profit_sharing import ProfitSharingSystem
from decimal import Decimal

async def setup_copy_trading_system():
    # 1. Создаем движок копитрейдинга
    copy_engine = CopyTradingEngine()

    # 2. Добавляем копировщиков
    copier1 = CopierAccount(
        user_id=1,
        email="copier1@example.com",
        api_key="key1",
        api_secret="secret1",
        position_mode='fixed_ratio',
        copy_ratio=Decimal('0.1')
    )
    copier1.settings = {
        'copy_ratio': 0.1,
        'max_position_size': 1000,
        'max_open_positions': 10,
        'max_daily_loss': -500
    }
    copy_engine.add_copier(copier1)

    copier2 = CopierAccount(
        user_id=2,
        email="copier2@example.com",
        api_key="key2",
        api_secret="secret2",
        position_mode='fixed_amount',
        fixed_amount=Decimal('100')
    )
    copier2.settings = {
        'fixed_amount': 100,
        'max_position_size': 500,
        'max_open_positions': 5,
        'max_daily_loss': -200
    }
    copy_engine.add_copier(copier2)

    # 3. Подключаем к существующему боту
    adapter = TradingBotAdapter(your_bot)
    await adapter.connect_to_existing_bot()
    adapter.copy_trading_engine = copy_engine

    # 4. Запускаем обработку
    await copy_engine.start_polling()

    # 5. Настраиваем систему монетизации
    profit_system = ProfitSharingSystem(db)

    # Периодический расчет прибыли (каждую неделю)
    while True:
        await asyncio.sleep(7 * 24 * 3600)  # 7 дней

        for copier in copy_engine.active_copiers.values():
            settlement = await profit_system.calculate_weekly_settlement(
                copier.user_id
            )
            print(f"Settlement for {copier.email}: ${settlement['total_payment']:.2f}")

if __name__ == "__main__":
    asyncio.run(setup_copy_trading_system())
```

---

## 📞 Поддержка

**Telegram**: @your_support_bot
**Email**: support@example.com
**Документация**: https://docs.example.com

---

**© 2025 Crypto Trading Bot v12 - Copy Trading System**
