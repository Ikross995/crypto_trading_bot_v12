# 🎓 Advanced GRU Training Guide

## 📋 Содержание

1. [Вариант Б: Дотренировка (Fine-tuning)](#вариант-б-дотренировка)
2. [Вариант В: Автоматическое переобучение](#вариант-в-автоматическое-переобучение)
3. [Вариант Г: Онлайн-обучение](#вариант-г-онлайн-обучение)

---

## 🔄 Вариант Б: Дотренировка (Fine-tuning)

### Что это?
Дотренировка существующей модели на **свежих данных** без потери уже выученных паттернов.

### Когда использовать?
- Раз в неделю/месяц для обновления модели
- После значительных изменений рынка
- Когда точность модели снизилась

### Установка зависимостей
```bash
# Все зависимости уже установлены из основного обучения
```

### Использование

#### Простейший вариант (рекомендуется)
```powershell
# Дотренировать на последних 30 днях (5 эпох)
python scripts\finetune_gru.py --days 30 --epochs 5
```

#### С параметрами
```powershell
# Дотренировать на последних 60 днях (10 эпох)
python scripts\finetune_gru.py --days 60 --epochs 10

# Дотренировать только на BTC
python scripts\finetune_gru.py --symbols BTCUSDT --days 90 --epochs 15

# Использовать другой learning rate
python scripts\finetune_gru.py --days 30 --lr 0.00005

# Сохранить в отдельный файл (не перезаписывать)
python scripts\finetune_gru.py --days 30 --save-as models\checkpoints\gru_finetuned.pt
```

### Рекомендуемое расписание
```
Раз в неделю: --days 30 --epochs 5
Раз в месяц: --days 90 --epochs 10
После крупных событий: --days 14 --epochs 7
```

### Время выполнения
- **30 дней данных**: ~5-10 минут
- **60 дней данных**: ~10-15 минут
- **90 дней данных**: ~15-20 минут

---

## ⏰ Вариант В: Автоматическое переобучение

### Что это?
Автоматическая дотренировка модели **по расписанию** без вашего участия.

### Когда использовать?
- Для полностью автоматизированного бота
- Когда хотите "поставить и забыть"
- Для постоянного поддержания актуальности модели

### Установка зависимостей
```powershell
pip install schedule
```

### Использование

#### Запуск по расписанию

**Еженедельно (каждое воскресенье в 02:00):**
```powershell
python scripts\auto_retrain_gru.py
```

**Ежедневно в 03:00:**
```powershell
python scripts\auto_retrain_gru.py --schedule daily --time 03:00
```

**Ежемесячно (1-го числа в 02:00):**
```powershell
python scripts\auto_retrain_gru.py --schedule monthly --time 02:00
```

**С кастомными параметрами:**
```powershell
python scripts\auto_retrain_gru.py --schedule weekly --days 45 --epochs 7
```

#### Запустить сейчас (без расписания)
```powershell
python scripts\auto_retrain_gru.py --run-now
```

### Запуск в фоне (Windows)

**Вариант 1: PowerShell фон**
```powershell
Start-Process python -ArgumentList "scripts\auto_retrain_gru.py" -WindowStyle Hidden
```

**Вариант 2: Планировщик заданий Windows**
1. Откройте "Планировщик заданий" (Task Scheduler)
2. Создайте новое задание
3. Триггер: Еженедельно, воскресенье, 02:00
4. Действие: Запустить программу
   - Программа: `C:\Users\User\miniconda3\envs\ai_trading\python.exe`
   - Аргументы: `scripts\auto_retrain_gru.py --run-now`
   - Рабочая папка: `C:\Users\User\crypto_trading_bot_v12`

### Что происходит при автопереобучении?

1. ✅ **Создаётся бэкап** старой модели в `models/backups/`
2. 📥 **Загружаются** последние 30 дней данных
3. 🎓 **Дотренировка** 5 эпох (~10 минут)
4. 💾 **Сохранение** обновлённой модели
5. 🗑️ **Удаление** старых бэкапов (оставляет последние 5)

### Логи
Все действия записываются в `logs/auto_retrain.log`

```powershell
# Смотреть логи в реальном времени
Get-Content logs\auto_retrain.log -Wait
```

---

## 🧠 Вариант Г: Онлайн-обучение

### Что это?
Модель **учится на каждой вашей сделке** в реальном времени.

### Как работает?
```
1. Бот открывает сделку с прогнозом GRU: "BTC вырастет до $43,500"
2. Сделка закрывается: "BTC реально достиг $43,420"
3. Модель видит ошибку: -$80
4. Модель корректирует веса чтобы в следующий раз быть точнее
```

### Преимущества
- ✅ Адаптация к **вашему стилю** трейдинга
- ✅ Учёт **ваших настроек** (стопы, тейк-профиты)
- ✅ Постоянное улучшение **без вашего участия**
- ✅ Модель становится **персональной** (заточена под вас)

### Настройка

#### 1. Включите в .env
```bash
# Онлайн-обучение
ONLINE_LEARNING_ENABLE=true
ONLINE_LEARNING_LR=0.00001  # Очень низкий для стабильности
ONLINE_LEARNING_MIN_TRADES=10  # Минимум сделок перед сохранением
ONLINE_LEARNING_SAVE_INTERVAL=50  # Сохранять каждые 50 сделок
```

#### 2. Интеграция уже готова!
Онлайн-обучение автоматически активируется когда:
- ✅ GRU модель загружена
- ✅ `ONLINE_LEARNING_ENABLE=true` в .env
- ✅ Бот открывает и закрывает сделки

### Как это работает в боте?

**При открытии сделки:**
```python
# runner/live.py автоматически:
1. Запоминает прогноз GRU
2. Запоминает входные данные (60 свечей)
3. Ждёт закрытия сделки
```

**При закрытии сделки:**
```python
# runner/live.py автоматически:
1. Сравнивает прогноз с реальностью
2. Добавляет опыт в буфер
3. Обучает модель на этом опыте
4. Каждые 50 сделок - сохраняет модель
```

### Мониторинг

**В логах бота будет:**
```
🎓 Online learning update #15: Loss=0.000234, MAE=23.45, Batch size=16
📝 Experience added: Pred=43500.00, Actual=43420.00, Error=80.00, Profit=$12.50
💾 Model auto-saved after 50 online updates (Avg MAE: 34.21)
```

### Статистика онлайн-обучения

**Проверить статистику:**
```python
# В Python консоли или боте
from utils.online_learning import OnlineLearner

stats = online_learner.get_statistics()
print(stats)
# {
#   'total_updates': 127,
#   'buffer_size': 100,
#   'avg_recent_loss': 0.000156,
#   'avg_recent_mae': 28.34,
#   'learning_rate': 0.00001
# }
```

### Важные параметры

**ONLINE_LEARNING_LR** (Learning Rate):
- `0.00001` - Очень консервативно (рекомендуется)
- `0.00005` - Быстрое обучение (риск нестабильности)
- `0.000001` - Сверх-консервативно

**ONLINE_LEARNING_SAVE_INTERVAL**:
- `50` - Сохранять каждые 50 сделок (рекомендуется)
- `100` - Реже сохранять (меньше нагрузка на диск)
- `20` - Чаще сохранять (безопаснее, но больше I/O)

### Резервные копии
При онлайн-обучении модель постоянно обновляется. Рекомендуется:

```powershell
# Раз в неделю создавайте ручной бэкап
copy models\checkpoints\gru_model_pytorch.pt models\backups\manual_backup_$(Get-Date -Format "yyyyMMdd").pt
```

---

## 🔥 Комбинированная стратегия (рекомендуется!)

Используйте все три варианта вместе для максимальной эффективности:

### Недельный цикл:
```
Понедельник-Суббота:
  ✅ Онлайн-обучение работает постоянно
  ✅ Модель учится на каждой сделке

Воскресенье в 02:00:
  ✅ Автоматическое переобучение (30 дней)
  ✅ Создаётся бэкап старой модели
```

### Месячный цикл:
```
1-30 число:
  ✅ Онлайн-обучение + еженедельное авто-переобучение

1-го числа:
  ✅ Ручная дотренировка на 90 днях (глубокое обновление)
  python scripts\finetune_gru.py --days 90 --epochs 15
```

### Преимущества комбинации:
1. **Онлайн-обучение**: Быстрая адаптация к вашим сделкам
2. **Авто-переобучение**: Регулярное обновление свежими данными
3. **Ручная дотренировка**: Глубокие периодические обновления

---

## 📊 Сравнение вариантов

| Параметр | Вариант Б (Fine-tune) | Вариант В (Auto) | Вариант Г (Online) |
|----------|----------------------|------------------|-------------------|
| **Частота** | Вручную | По расписанию | Каждая сделка |
| **Время** | 5-20 минут | 10 минут/неделя | Мгновенно |
| **Данные** | Исторические | Свежие исторические | Ваши сделки |
| **Адаптация** | Медленная | Средняя | Быстрая |
| **Участие** | Ручное | Автоматическое | Автоматическое |
| **Риск** | Низкий | Низкий | Средний* |

*При правильных настройках (низкий LR) риск минимален

---

## 🛠️ Troubleshooting

### Проблема: "Model not found"
**Решение:**
```powershell
# Убедитесь что модель обучена
python examples\gru_training_pytorch.py
```

### Проблема: "schedule module not found"
**Решение:**
```powershell
pip install schedule
```

### Проблема: Онлайн-обучение не работает
**Решение:**
1. Проверьте `.env`: `ONLINE_LEARNING_ENABLE=true`
2. Убедитесь что GRU включён: `GRU_ENABLE=true`
3. Проверьте логи бота на наличие ошибок

### Проблема: Модель стала хуже после онлайн-обучения
**Решение:**
```powershell
# Восстановите из бэкапа
copy models\backups\gru_model_backup_YYYYMMDD_HHMMSS.pt models\checkpoints\gru_model_pytorch.pt

# Уменьшите learning rate в .env
ONLINE_LEARNING_LR=0.000001
```

---

## 📞 Поддержка

Если возникли вопросы:
1. Проверьте логи: `logs/auto_retrain.log`
2. Посмотрите метрики в логах бота
3. Проверьте бэкапы в `models/backups/`

---

## 🎯 Рекомендации

**Для новичков:**
- Начните с **Варианта Б** (ручная дотренировка раз в неделю)

**Для опытных:**
- Используйте **Вариант В** (автоматическое переобучение)

**Для профи:**
- Включайте все три варианта + комбинированная стратегия

---

**Удачи в обучении! 🚀**
