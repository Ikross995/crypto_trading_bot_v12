# 🔧 Решение CUDA ошибки при обучении

## 📊 Текущая ситуация

**Успешно обучено:** 5/10 символов
- ✅ BNBUSDT (WR=56%, **Sharpe=8.36** 🏆)
- ✅ ETHUSDT (WR=53%, **Sharpe=5.58** 🏆)
- ✅ BTCUSDT (WR=56%, Sharpe=1.53)
- ⚠️ SOLUSDT (WR=50%, Sharpe=-5.63)
- ⚠️ ADAUSDT (WR=38%, Sharpe=-14.84)

**Упали с CUDA ошибкой:** 5/10 символов
- ❌ XRPUSDT
- ❌ DOGEUSDT
- ❌ AVAXUSDT
- ❌ LINKUSDT
- ❌ APTUSDT

**Причина:** После 155 минут обучения GPU перегрелся или память переполнилась.

---

## 🚀 Решение 1: Использовать только успешные модели (РЕКОМЕНДУЮ)

У вас уже есть **3 отличные модели** с хорошими показателями!

### Запуск продакшн-бота:

```bash
# Запустить с лучшими 3 парами
./start_combo_best_only.sh

# Или вручную
python cli.py live --use-combo --symbols BNBUSDT,ETHUSDT,BTCUSDT
```

### Почему этого достаточно:

1. **BNBUSDT** - Sharpe 8.36 (отлично!)
2. **ETHUSDT** - Sharpe 5.58 (отлично!)
3. **BTCUSDT** - Sharpe 1.53 (хорошо)

Этих трех пар достаточно для диверсификации и прибыли.

---

## 🔧 Решение 2: Переобучить упавшие символы на CPU

Если нужны все 10 пар, переобучите упавшие на CPU (медленнее, но безопасно):

```bash
# Запустить переобучение на CPU
python retrain_failed_cpu.py
```

Это займет ~2-3 часа (без GPU), но гарантированно сработает.

---

## 🐛 Решение 3: Debug CUDA ошибки

Если хотите понять причину CUDA ошибки:

```bash
# Включить CUDA debugging
export CUDA_LAUNCH_BLOCKING=1
export TORCH_USE_CUDA_DSA=1

# Попробовать переобучить один символ
python -c "
import asyncio
from run_full_combo_system_multi import train_single_symbol

asyncio.run(train_single_symbol('XRPUSDT', days=365, interval='30m', quick_mode=True))
"
```

---

## 🎯 Рекомендация

**Используйте Решение 1** - у вас уже есть отличные модели!

Запустите бота с 3 лучшими парами:
```bash
./start_combo_best_only.sh
```

Если позже понадобятся остальные пары - переобучите на CPU.

---

## 📈 Ожидаемые результаты

При использовании топ-3 моделей:
- **BNBUSDT**: Средняя прибыль ~8.36 Sharpe (отлично)
- **ETHUSDT**: Средняя прибыль ~5.58 Sharpe (отлично)
- **BTCUSDT**: Средняя прибыль ~1.53 Sharpe (хорошо)

**Средний Sharpe: ~5.16** - это очень хороший показатель!
