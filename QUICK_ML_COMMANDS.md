# 🚀 Quick ML Commands

## 📊 Проверить статус ML моделей
```bash
python3 check_ml_status.py
```

**Покажет:**
- ✅ Статус каждой модели (обучена/не обучена)
- 📊 Количество samples и прогресс обучения
- 🎯 Фазу обучения (COLD START / LEARNING / FULL ML)
- 💡 Рекомендации по улучшению

---

## 🧪 Протестировать персистентность
```bash
python3 test_ml_persistence.py
```

**Проверит:**
- ✅ Модели сохраняются правильно
- ✅ Модели загружаются между запусками
- ✅ Samples накапливаются (не сбрасываются)
- ✅ Предсказания работают с загруженными моделями

---

## 🔍 Посмотреть сохраненные файлы
```bash
ls -lh ml_learning_data/
```

**Покажет:**
- Model files (`.pkl`) - обученные модели
- Scaler files (`.pkl`) - нормализация данных
- Metadata files (`.json`) - samples_seen, timestamp
- Trade outcomes и market contexts

---

## 📈 Мониторинг в реальном времени
```bash
# Запустить бота (интерактивно)
python3 start_bot.py
# Выберите: 1 (Testnet) или 2 (Real)

# ИЛИ прямой запуск через CLI
python3 cli.py live --testnet

# В другом терминале - мониторинг
watch -n 10 python3 check_ml_status.py
```

---

## 🗑️ Очистить модели (начать заново)
```bash
rm -rf ml_learning_data/ enhanced_learning_data/
```

**Внимание:** Все обученные модели будут удалены!

---

## 📝 Логи ML системы
```bash
# При старте бота
grep "ML_LOAD" bot.log

# При обучении
grep "ML_LEARNING\|ML_SAMPLES" bot.log

# При автосохранении
grep "AUTO_SAVE" bot.log

# При остановке
grep "SHUTDOWN.*ML" bot.log
```

---

## 🎯 Ожидаемые фазы обучения

### Phase 1: COLD START (0-50 samples)
```
🧠 [COLD_START] Learning mode: 15/50 samples
```
- ML собирает базовые данные
- Все IMBA сигналы проходят
- ML только наблюдает

### Phase 2: LEARNING (50-200 samples)
```
🎓 [LEARNING] Learning mode: 120/200 samples, progress: 46.7%
```
- ML начинает влиять на решения
- Адаптивные пороги сигналов
- Постепенное увеличение веса ML

### Phase 3: FULL ML (200+ samples)
```
🧠 [FULL_ML] Full ML mode: 250 samples
```
- ML полностью активна
- Оптимизация размера позиций
- Предсказательная фильтрация

---

## 💾 Backup моделей
```bash
# Создать backup
tar -czf ml_backup_$(date +%Y%m%d).tar.gz ml_learning_data/

# Восстановить backup
tar -xzf ml_backup_20251119.tar.gz
```

---

## 🔧 Troubleshooting

### Модели не сохраняются?
```bash
# Проверить права
ls -la ml_learning_data/

# Проверить логи
grep "ML_SAVE\|AUTO_SAVE" bot.log
```

### Модели не загружаются?
```bash
# Проверить файлы
ls ml_learning_data/*.pkl

# Проверить метаданные
cat ml_learning_data/pnl_predictor_metadata.json
```

### TensorFlow не работает?
```bash
python3 -c "import tensorflow as tf; print(tf.__version__)"
```

---

## 📚 Полная документация
```bash
cat ML_PERSISTENCE_GUIDE.md
```
