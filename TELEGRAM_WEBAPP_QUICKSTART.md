# 🚀 Быстрый старт Telegram Web App

## Проблема решена: Кнопка веб-приложения теперь появится!

Раньше бот отправлял только уведомления. Теперь у тебя будет интерактивный дашборд прямо в Telegram!

---

## ⚡ Быстрая настройка за 3 минуты

### 1️⃣ Установи ngrok

Скачай: https://ngrok.com/download

Или через терминал:
```bash
# Linux/Mac
brew install ngrok

# Или скачай binary и распакуй
```

### 2️⃣ Запусти веб-сервер

В первом терминале:
```bash
python webapp_server.py
```

Сервер запустится на `http://localhost:8080`

### 3️⃣ Запусти ngrok

Во втором терминале:
```bash
ngrok http 8080
```

Ты увидишь что-то типа:
```
Forwarding   https://abc123.ngrok.io -> http://localhost:8080
```

**Скопируй этот HTTPS URL!**

### 4️⃣ Добавь URL в .env

Открой файл `.env` и найди строку:
```bash
TG_WEBAPP_URL=
```

Вставь туда URL из ngrok:
```bash
TG_WEBAPP_URL=https://abc123.ngrok.io/
```

**ВАЖНО:** Не забудь `/` в конце!

### 5️⃣ Перезапусти бота

Останови бота (Ctrl+C) и запусти снова:
```bash
python cli.py live --timeframe 15m --testnet --symbols BTCUSDT,ETHUSDT,SOLUSDT
```

### 6️⃣ Проверь в Telegram!

1. Открой бота в Telegram
2. Отправь команду `/menu`
3. Теперь должна появиться кнопка **📱 Интерактивный Dashboard**!
4. Нажми на нее - откроется красивый веб-дашборд прямо в Telegram!

---

## 🎯 Что ты увидишь:

- 💰 Баланс и equity в реальном времени
- 📊 Графики P&L и equity curve
- 📈 Открытые позиции с деталями
- 🔥 Статистика сделок и win rate
- 🎨 Красивый UI в стиле Telegram

---

## 🔧 Если не работает:

### Кнопка не появляется?
✅ Проверь что `TG_WEBAPP_URL` добавлен в `.env`
✅ Перезапусти бота после изменений в `.env`
✅ URL должен начинаться с `https://`

### Кнопка есть, но дашборд не открывается?
✅ Проверь что `webapp_server.py` запущен
✅ Проверь что ngrok работает
✅ Попробуй открыть URL в браузере - должен открыться дашборд

### Нет данных в дашборде?
✅ Запусти бота и подожди 5 минут - данные обновятся автоматически
✅ Проверь файл `data/dashboard_state.json` - он должен появиться

---

## 💡 Бонус: Автоматическая настройка

Создай скрипт `start_webapp.sh`:
```bash
#!/bin/bash
# Запуск веб-сервера в фоне
python webapp_server.py &
WEB_PID=$!

# Запуск ngrok в фоне
ngrok http 8080 &
NGROK_PID=$!

echo "✅ Web server started (PID: $WEB_PID)"
echo "✅ Ngrok started (PID: $NGROK_PID)"
echo ""
echo "📋 Получи URL из ngrok:"
echo "   curl http://localhost:4040/api/tunnels | jq -r '.tunnels[0].public_url'"
echo ""
echo "🛑 Для остановки:"
echo "   kill $WEB_PID $NGROK_PID"
```

Сделай его исполняемым:
```bash
chmod +x start_webapp.sh
```

Запускай:
```bash
./start_webapp.sh
```

---

## 📚 Подробная инструкция

Полная документация: `TELEGRAM_WEBAPP_SETUP.md`

---

## ❓ Вопросы?

- Бот отправляет только уведомления → Проверь что `TG_WEBAPP_URL` настроен
- Кнопка есть, но дашборд пустой → Подожди 5 минут, данные обновятся
- ngrok URL изменился → Обнови `TG_WEBAPP_URL` в `.env` и перезапусти бота

---

## 🎉 Готово!

Теперь у тебя есть полноценный интерактивный дашборд прямо в Telegram!

**Наслаждайся торговлей! 🚀**
