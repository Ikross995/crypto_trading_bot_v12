# 🔄 Standalone Copy Trader

Автоматическое копирование торговых сигналов через Telegram независимо от основного бота.

## 📋 Оглавление

- [Что это?](#что-это)
- [Два режима работы](#два-режима-работы)
- [Быстрый старт](#быстрый-старт)
- [Детальная настройка](#детальная-настройка)
- [Примеры использования](#примеры-использования)
- [FAQ](#faq)

---

## Что это?

**Standalone Copy Trader** - это независимый модуль, который можно запускать отдельно от основного торгового бота. Он позволяет:

1. **Follower Mode**: Копировать сделки из Telegram канала
2. **Leader Mode**: Публиковать свои сделки в Telegram канал

### Преимущества standalone подхода:

✅ **Независимость** - работает без основного бота
✅ **Простота** - одна команда для запуска
✅ **Гибкость** - можно запускать несколько экземпляров
✅ **Надёжность** - отдельные логи и мониторинг

---

## Два режима работы

### 🎯 Follower Mode (Копирование)

```
Telegram канал → Парсинг сигналов → Binance API → Копирование сделок
```

**Что делает:**
- Слушает Telegram канал/группу
- Парсит торговые сигналы (LONG/SHORT, ENTRY, TP, SL)
- Копирует сделки через Binance Futures API
- Применяет риск-менеджмент и лимиты

**Для кого:**
- Трейдеры, которые хотят копировать чужие сигналы
- Автоматизация ручной торговли
- Портфельная диверсификация

### 📢 Leader Mode (Публикация)

```
Основной бот → HTTP API → Leader → Telegram канал
```

**Что делает:**
- Принимает сигналы от основного бота через HTTP API
- Форматирует их для копирования
- Публикует в Telegram канал
- Ведёт статистику

**Для кого:**
- Трейдеры, которые хотят делиться своими сигналами
- Создание платных каналов с сигналами
- Команды трейдеров

---

## Быстрый старт

### Предварительные требования

```bash
# Python 3.8+
python --version

# Установка зависимостей
pip install python-binance aiohttp loguru python-dotenv
```

### Follower Mode (5 минут)

**Шаг 1: Создайте конфигурацию**

```bash
cp .env.follower.example .env.follower
```

**Шаг 2: Отредактируйте `.env.follower`**

```bash
# Минимальная конфигурация
TG_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TG_SOURCE_CHANNEL=@trading_signals_pro
BINANCE_API_KEY=your_api_key
BINANCE_API_SECRET=your_api_secret
TESTNET=true  # Сначала тестируем!
```

**Шаг 3: Запустите**

```bash
python start_copy_trader.py --mode follower --env .env.follower --testnet
```

**Готово!** 🎉 Копи-трейдер запущен и слушает сигналы.

---

### Leader Mode (5 минут)

**Шаг 1: Создайте Telegram канал**

1. Откройте Telegram → New Channel
2. Название: "My Trading Signals"
3. Тип: Public
4. Username: `@my_trading_signals`

**Шаг 2: Добавьте бота в канал**

1. Добавьте вашего бота как администратора
2. Дайте права: Post Messages, Edit Messages

**Шаг 3: Создайте конфигурацию**

```bash
cp .env.leader.example .env.leader
```

**Шаг 4: Отредактируйте `.env.leader`**

```bash
TG_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TG_CHAT_ID=@my_trading_signals
LEADER_PORT=8080
```

**Шаг 5: Запустите**

```bash
python start_copy_trader.py --mode leader --env .env.leader
```

**Готово!** 🎉 Leader запущен на порту 8080.

---

## Детальная настройка

### Follower Mode - Настройки риск-менеджмента

#### Размер позиции

```bash
# Копировать 50% от размера лидера
COPY_POSITION_SIZE_MULTIPLIER=0.5

# Минимум и максимум в USDT
COPY_MIN_POSITION_SIZE=10.0
COPY_MAX_POSITION_SIZE=100.0
```

**Пример:**
- Лидер открывает позицию $1000
- Ваш multiplier = 0.5
- Вы откроете $500
- Но не больше чем MAX ($100) → **откроете $100**

#### Лимиты

```bash
# Максимум открытых позиций одновременно
COPY_MAX_OPEN_POSITIONS=5

# Дневной лимит убытков (% от баланса)
COPY_DAILY_LOSS_LIMIT_PCT=5.0

# Максимальное плечо
COPY_MAX_LEVERAGE=10
```

**Пример работы дневного лимита:**
- Баланс: $1000
- Лимит: 5%
- Максимальный дневной убыток: $50
- Если убыток за день превысил $50 → новые позиции не открываются до следующего дня (UTC)

#### Фильтрация символов

```bash
# Копировать только эти символы
COPY_ALLOWED_SYMBOLS=BTCUSDT,ETHUSDT,BNBUSDT

# Или оставьте пустым для копирования всех
COPY_ALLOWED_SYMBOLS=
```

### Поддерживаемые форматы сигналов

Follower понимает несколько форматов:

#### Формат 1: Стандартный

```
🟢 LONG BTCUSDT

Entry: $41,250.00
Leverage: 10x
Position: $100.00

TP1: $42,780.00 (+3.71%)
TP2: $43,500.00 (+5.45%)
SL: $40,435.00 (-1.98%)

#BTCUSDT #LONG #CopyTrade
```

#### Формат 2: Компактный

```
🟢 LONG BTCUSDT
Entry: 41250 | TP: 42780 | SL: 40435 | Lev: 10x
```

#### Формат 3: Закрытие

```
❌ CLOSE BTCUSDT
Profit: +5.2%
```

---

## Примеры использования

### Сценарий 1: Тестирование на Testnet

```bash
# Запуск follower на testnet
python start_copy_trader.py \
  --mode follower \
  --env .env.follower \
  --testnet \
  --verbose
```

**Что происходит:**
- Подключение к Binance Testnet
- Подробные логи (--verbose)
- Копирование сигналов БЕЗ реальных денег

### Сценарий 2: Dry Run (парсинг без выполнения)

```bash
# Только парсинг, без выполнения ордеров
python start_copy_trader.py \
  --mode follower \
  --env .env.follower \
  --dry-run
```

**Для чего:**
- Проверка парсинга сигналов
- Тестирование логики
- Проверка подключения к каналу

### Сценарий 3: Реальная торговля

```bash
# В .env.follower установите:
TESTNET=false
DRY_RUN=false

# Запуск
python start_copy_trader.py \
  --mode follower \
  --env .env.follower
```

⚠️ **ВНИМАНИЕ**: Это реальная торговля с реальными деньгами!

### Сценарий 4: Несколько источников

Запустите несколько экземпляров для копирования из разных каналов:

```bash
# Терминал 1: Копирование из канала A
python start_copy_trader.py --mode follower --env .env.follower.channelA

# Терминал 2: Копирование из канала B
python start_copy_trader.py --mode follower --env .env.follower.channelB
```

### Сценарий 5: Leader + Основной бот

**Терминал 1: Запустите Leader**
```bash
python start_copy_trader.py --mode leader --env .env.leader
```

**Терминал 2: Запустите основной бот**
```bash
python cli.py live \
  --timeframe 30m \
  --testnet \
  --use-combo \
  --symbols BTCUSDT,ETHUSDT
```

**Интеграция:**
Добавьте в `runner/live.py` вызовы Leader API при открытии/закрытии позиций.

---

## FAQ

### Q: Безопасно ли использовать Copy Trader?

**A:** Да, при соблюдении правил:
1. ✅ Начните с Testnet
2. ✅ Используйте консервативные лимиты риска
3. ✅ Установите дневной лимит убытков
4. ✅ Начните с малых сумм
5. ✅ Проверьте источник сигналов

### Q: Какие комиссии?

**A:**
- Binance комиссия: ~0.04% за сделку (Futures)
- Copy Trader: бесплатно, open source
- Telegram: бесплатно

### Q: Можно ли копировать частично?

**A:** Да! Используйте `COPY_POSITION_SIZE_MULTIPLIER`:
- 0.1 = 10% от размера лидера
- 0.5 = 50%
- 1.0 = 100% (полное копирование)

### Q: Что если пропущен сигнал?

**A:**
- Follower работает 24/7 и не пропускает сигналы
- Telegram хранит сообщения
- Если бот был выключен - пропущенные сигналы НЕ копируются (безопасность)

### Q: Работает ли с приватными каналами?

**A:** Да!
1. Получите ID канала через @getidsbot
2. Используйте ID вместо @username
3. Бот должен быть участником канала

### Q: Можно ли изменить размер TP/SL?

**A:** Сейчас копируются TP/SL из сигнала.
Кастомизация TP/SL - в планах на следующую версию.

### Q: Как остановить копи-трейдер?

**A:** Просто нажмите `Ctrl+C` в терминале.
Открытые позиции останутся открытыми, управляйте ими вручную через Binance.

### Q: Поддержка других бирж?

**A:** Сейчас только Binance Futures.
Планируется: Bybit, OKX.

---

## Логи и мониторинг

### Где найти логи?

```bash
# Логи сохраняются в директории logs/
ls -la logs/

# Последние логи
tail -f logs/copy_trader_2024-01-15.log
```

### Уровни логирования

```bash
# INFO - стандартный (рекомендуется)
python start_copy_trader.py --mode follower

# DEBUG - подробный (для отладки)
python start_copy_trader.py --mode follower --verbose
```

### Мониторинг Leader

Leader предоставляет HTTP API для мониторинга:

```bash
# Health check
curl http://localhost:8080/health

# Статистика
curl http://localhost:8080/stats
```

Ответ:
```json
{
  "started_at": "2024-01-15T10:30:00",
  "uptime_seconds": 3600,
  "signals_published": 15,
  "telegram_channel": "@my_signals"
}
```

---

## Безопасность

### ✅ Рекомендации

1. **API ключи**
   - Используйте API ключи только с правами Futures
   - Ограничьте IP адреса (если возможно)
   - НЕ давайте права на вывод средств

2. **Риск-менеджмент**
   - Установите `COPY_DAILY_LOSS_LIMIT_PCT` (рекомендуется 5%)
   - Ограничьте `COPY_MAX_POSITION_SIZE`
   - Установите `COPY_MAX_OPEN_POSITIONS`

3. **Тестирование**
   - Всегда тестируйте на Testnet
   - Используйте `--dry-run` для проверки
   - Начинайте с малых сумм

4. **Мониторинг**
   - Проверяйте логи регулярно
   - Отслеживайте дневной PnL
   - Следите за балансом

### ❌ Чего НЕ делать

- ❌ Не копируйте сигналы из непроверенных источников
- ❌ Не используйте весь баланс
- ❌ Не отключайте риск-лимиты
- ❌ Не запускайте сразу на mainnet
- ❌ Не используйте высокое плечо (>10x)

---

## Поддержка

### Проблемы и баги

Если что-то не работает:

1. Проверьте логи: `tail -f logs/copy_trader_*.log`
2. Запустите с `--verbose` для подробных логов
3. Проверьте `.env` конфигурацию
4. Убедитесь что зависимости установлены

### Полезные команды

```bash
# Проверка зависимостей
pip list | grep -E "binance|aiohttp|loguru"

# Тест подключения к Telegram
curl "https://api.telegram.org/bot<TOKEN>/getMe"

# Тест Binance API
python -c "from binance.client import Client; print('OK')"
```

---

## Roadmap

### Планируется:

- [ ] Поддержка Bybit и OKX
- [ ] Кастомные TP/SL уровни
- [ ] Trailing Stop Loss
- [ ] Web dashboard для мониторинга
- [ ] Автоматическое определение формата сигналов
- [ ] Поддержка нескольких аккаунтов Binance
- [ ] Уведомления в Telegram о копировании
- [ ] Статистика и аналитика
- [ ] Backtesting на исторических сигналах

---

## Лицензия

MIT License - используйте свободно и на свой риск.

---

## Disclaimer

⚠️ **ВАЖНО**: Торговля криптовалютами сопряжена с высокими рисками. Этот софт предоставляется "как есть" без каких-либо гарантий. Используйте на свой страх и риск. Авторы не несут ответственности за ваши убытки.

**Рекомендации:**
- Инвестируйте только те средства, которые можете позволить себе потерять
- Проводите собственное исследование (DYOR)
- Тестируйте на Testnet перед реальной торговлей
- Используйте консервативный риск-менеджмент

---

Удачной торговли! 🚀
