# 🔄 Copy Trading Setup Guide

## Полная настройка копи-трейдинга для AI Crypto Trading Bot

Этот гайд покажет как:
1. **Leader Mode** - публиковать сигналы вашего бота для копирования
2. **Follower Mode** - автоматически копировать сигналы от других трейдеров

---

## 📊 Два режима работы

### 🎯 Leader Mode (Публикация сигналов)
Ваш бот отправляет свои сделки в Telegram канал/группу, откуда их могут копировать другие трейдеры.

**Что публикуется:**
- Сигналы на вход (символ, направление, цена входа, TP, SL)
- Обновления позиций (частичные закрытия, перенос SL)
- Сигналы на выход (закрытие позиций с результатом)
- Статистика производительности

### 👥 Follower Mode (Копирование сигналов)
Ваш бот слушает Telegram канал и автоматически копирует сделки от выбранного трейдера.

**Что копируется:**
- Входы в позиции (с настраиваемым размером)
- Take Profit и Stop Loss уровни
- Частичные закрытия
- Полные закрытия позиций

---

## 🚀 Быстрый старт: Leader Mode (15 минут)

### Шаг 1: Создайте публичный Telegram канал

1. Откройте Telegram → New Channel
2. Название: `My Trading Signals` (или любое другое)
3. Описание: Опишите вашу стратегию
4. Тип: **Public** (для максимального охвата)
5. Username: `@my_trading_signals` (уникальный)

**Или создайте приватную группу:**
1. New Group → Add Members
2. Добавьте вашего бота как администратора
3. Получите Chat ID через @userinfobot

### Шаг 2: Настройте бота как администратора

1. Добавьте вашего бота в канал как Administrator
2. Дайте права:
   - ✅ Post Messages
   - ✅ Edit Messages (для обновлений)
   - ✅ Delete Messages (для очистки)

### Шаг 3: Получите Channel ID

**Для публичных каналов:**
```bash
# Username канала (например: @my_trading_signals)
TG_CHAT_ID=@my_trading_signals
```

**Для приватных каналов:**
1. Перешлите любое сообщение из канала боту [@getidsbot](https://t.me/getidsbot)
2. Или откройте: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
3. Найдите `"chat":{"id":-1001234567890}`

### Шаг 4: Настройте .env

```bash
# ==========================================
# 📢 COPY TRADING - LEADER MODE
# ==========================================

# Ваш bot token от @BotFather
TG_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# ID канала для публикации сигналов
TG_CHAT_ID=@my_trading_signals

# Включить публикацию сигналов
TG_TRADE_NOTIFICATIONS=true

# Формат сигналов для копирования (подробный)
TG_COPY_TRADING_MODE=leader

# Включить публичную статистику
TG_PUBLISH_STATS=true

# Публиковать обновления каждые 6 часов
TG_DASHBOARD_ENABLED=true
TG_DASHBOARD_INTERVAL=21600
```

### Шаг 5: Запустите бота

```bash
python start_bot.py
```

**Готово!** Теперь все сделки вашего бота публикуются в канал в формате:

```
🟢 LONG BTCUSDT

📊 ENTRY INFO
Entry Price: $41,250.00
Leverage: 10x
Position Size: $100.00

🛡️ PROTECTION SETUP
💎 Take Profit: $42,780.00
   Target: +3.71% → ROI: ~37.1%

🛡️ Stop Loss: $40,435.00
   Distance: -1.98% → Risk: ~19.8%

💰 ACCOUNT STATUS
Balance: 1,234.56 USDT

📡 IMBA Signal Confidence: 85%
```

---

## 👥 Быстрый старт: Follower Mode (20 минут)

### Шаг 1: Подпишитесь на сигнальный канал

1. Найдите проверенный Telegram канал с торговыми сигналами
2. Убедитесь, что сигналы публикуются в **стандартном формате**
3. Подпишитесь на канал

**Рекомендуемый формат сигналов:**
```
LONG/SHORT SYMBOL
Entry: $XXXX
TP1: $XXXX
TP2: $XXXX
SL: $XXXX
Leverage: XXx
```

### Шаг 2: Создайте бота для мониторинга

Вам нужен **отдельный бот** для чтения сигналов:

1. Создайте нового бота через @BotFather
2. Назовите его `My Signal Reader`
3. Скопируйте токен

**Почему отдельный бот?**
- Ваш основной бот публикует сигналы
- Бот-читатель мониторит чужие сигналы
- Можно запустить несколько фолловер-ботов для разных каналов

### Шаг 3: Получите Channel ID источника

**Метод 1: Публичный канал**
```bash
TG_SOURCE_CHANNEL=@pro_trading_signals
```

**Метод 2: Приватный канал**
1. Добавьте бота-читателя в канал (администратор должен одобрить)
2. Получите ID через @getidsbot

### Шаг 4: Настройте копирование

Создайте отдельный `.env` файл для фолловер-бота:

```bash
# .env.follower
# ==========================================
# 📢 COPY TRADING - FOLLOWER MODE
# ==========================================

MODE=live
TESTNET=true  # Сначала на testnet!

# Ваши API ключи (для исполнения сделок)
BINANCE_API_KEY=your_api_key
BINANCE_API_SECRET=your_api_secret

# Bot token для чтения сигналов
TG_BOT_TOKEN=9876543210:ZYXwvuTSRqpONMlkJIhgFEdcba

# Канал источник сигналов
TG_SOURCE_CHANNEL=@pro_trading_signals

# Режим: копирование сигналов
TG_COPY_TRADING_MODE=follower

# Фильтр по автору (только от конкретного бота)
TG_COPY_ONLY_FROM_BOT=true
TG_SOURCE_BOT_USERNAME=@source_trading_bot

# ==========================================
# 💰 РИСК-МЕНЕДЖМЕНТ ДЛЯ КОПИРОВАНИЯ
# ==========================================

# Копировать с меньшим размером (50% от оригинала)
COPY_POSITION_SIZE_MULTIPLIER=0.5

# Максимальный размер позиции в USDT
COPY_MAX_POSITION_SIZE=50.0

# Минимальный размер позиции в USDT
COPY_MIN_POSITION_SIZE=10.0

# Копировать только определенные символы
COPY_ALLOWED_SYMBOLS=BTCUSDT,ETHUSDT,BNBUSDT,SOLUSDT

# Максимальное плечо (защита от рискованных сигналов)
COPY_MAX_LEVERAGE=10

# Использовать собственные TP/SL (игнорировать источник)
COPY_USE_OWN_EXITS=false

# Задержка копирования (мс) - для избежания проскальзывания
COPY_EXECUTION_DELAY_MS=500

# Максимальное отклонение цены от сигнала (%)
COPY_MAX_PRICE_DEVIATION_PCT=0.5

# Лимит открытых позиций
COPY_MAX_OPEN_POSITIONS=3

# Стоп копирование при дневном убытке (%)
COPY_DAILY_LOSS_LIMIT_PCT=5.0
```

### Шаг 5: Запустите фолловер-бота

```bash
# Используйте отдельный конфиг
python -c "
import sys
from pathlib import Path
sys.path.insert(0, str(Path.cwd()))

from core.config import Config
from runner.copy_trader import run_copy_trading
import asyncio

config = Config.from_env('.env.follower')
asyncio.run(run_copy_trading(config))
"
```

---

## 🔧 Продвинутые настройки

### Leader Mode: Настройка формата сигналов

```bash
# ==========================================
# LEADER MODE - ADVANCED
# ==========================================

# Публиковать сигналы с полными деталями
TG_SIGNAL_FORMAT=detailed  # detailed, compact, custom

# Включить публикацию причины входа
TG_INCLUDE_ENTRY_REASON=true

# Включить технический анализ в сигнал
TG_INCLUDE_TECHNICALS=true

# Публиковать обновления позиций (частичные закрытия)
TG_PUBLISH_POSITION_UPDATES=true

# Публиковать trailing stop обновления
TG_PUBLISH_TRAILING_UPDATES=false  # Может быть спамом

# Автоматически удалять старые сигналы
TG_AUTO_DELETE_OLD_SIGNALS=true
TG_SIGNAL_TTL_HOURS=24

# Публиковать ежедневную статистику
TG_PUBLISH_DAILY_STATS=true
TG_DAILY_STATS_TIME=23:59  # UTC

# Публиковать анализ лучших/худших сделок
TG_PUBLISH_TRADE_ANALYSIS=true
```

### Follower Mode: Умное копирование

```bash
# ==========================================
# FOLLOWER MODE - ADVANCED
# ==========================================

# Копировать только сигналы с высоким R:R
COPY_MIN_RISK_REWARD=1.5

# Копировать только Long/Short
COPY_ALLOWED_SIDES=LONG,SHORT  # or LONG only, SHORT only

# Отключить копирование в выходные
COPY_SKIP_WEEKENDS=false

# Копировать только во время высокой ликвидности
COPY_ONLY_HIGH_LIQUIDITY_HOURS=false
COPY_LIQUIDITY_HOURS=08:00-22:00  # UTC

# Подтверждение перед копированием (manual mode)
COPY_REQUIRE_CONFIRMATION=false

# Проверка сигнала собственной AI
COPY_USE_AI_FILTER=true
COPY_AI_MIN_CONFIDENCE=0.6

# Ждать подтверждения цены (не входить сразу)
COPY_WAIT_FOR_PRICE_CONFIRMATION=true
COPY_CONFIRMATION_CANDLES=2

# Копировать частичные закрытия
COPY_FOLLOW_PARTIAL_EXITS=true

# Копировать trailing stop обновления
COPY_FOLLOW_TRAILING_UPDATES=true

# Автоматическое закрытие при закрытии источником
COPY_AUTO_CLOSE_ON_SOURCE_CLOSE=true

# Закрывать при потере связи с источником
COPY_CLOSE_ON_SOURCE_TIMEOUT=true
COPY_SOURCE_TIMEOUT_MINUTES=30
```

---

## 📡 Форматы сигналов

### Стандартный формат (рекомендуется)

```
🟢 LONG BTCUSDT

Entry: $41,250.00
Leverage: 10x
Position: $100.00

TP1: $42,780.00 (+3.71%)
TP2: $43,500.00 (+5.45%)
SL: $40,435.00 (-1.98%)

#BTCUSDT #LONG #CopyTrade
```

### Компактный формат

```
🟢 LONG BTCUSDT
Entry: 41250 | TP: 42780 | SL: 40435 | Lev: 10x
```

### JSON формат (для программного парсинга)

```json
{
  "action": "OPEN",
  "side": "LONG",
  "symbol": "BTCUSDT",
  "entry_price": 41250.00,
  "take_profits": [42780.00, 43500.00],
  "stop_loss": 40435.00,
  "leverage": 10,
  "position_size_usdt": 100.00,
  "timestamp": "2025-11-16T12:34:56Z"
}
```

---

## 🛡️ Безопасность и риск-менеджмент

### ⚠️ ВАЖНО для Follower Mode

**НЕ КОПИРУЙТЕ вслепую!** Даже от проверенных источников.

✅ **Best Practices:**

1. **Тестируйте на Testnet:**
   ```bash
   TESTNET=true
   DRY_RUN=false  # Реальные ордера на testnet
   ```

2. **Начинайте с малого:**
   ```bash
   COPY_POSITION_SIZE_MULTIPLIER=0.1  # 10% от размера
   COPY_MAX_POSITION_SIZE=20.0        # Максимум $20
   ```

3. **Используйте стоп-лоссы:**
   ```bash
   COPY_ALWAYS_USE_SL=true
   COPY_MAX_SL_PCT=3.0  # Не более 3% риска
   ```

4. **Ограничьте риски:**
   ```bash
   COPY_MAX_OPEN_POSITIONS=3
   COPY_DAILY_LOSS_LIMIT_PCT=5.0
   COPY_MAX_LEVERAGE=10
   ```

5. **Фильтруйте сигналы:**
   ```bash
   COPY_MIN_RISK_REWARD=1.5
   COPY_USE_AI_FILTER=true
   ```

6. **Мониторьте производительность:**
   - Ведите журнал скопированных сделок
   - Сравнивайте с источником
   - Анализируйте проскальзывание

### 🚨 Red Flags (когда НЕ копировать)

❌ Слишком высокое плечо (>20x)
❌ Слишком большой размер позиции (>10% баланса)
❌ Отсутствие стоп-лоссов
❌ Сигналы без истории
❌ Unrealistic обещания (500%+ в месяц)
❌ Закрытая статистика
❌ Требование предоплаты без trial периода

---

## 📊 Создание собственного копи-трейдинг сервиса

### Leader Mode + Монетизация

Если ваш бот показывает хорошие результаты, можете монетизировать сигналы:

1. **Бесплатный канал** (публичный):
   - Публикуйте базовые сигналы
   - Показывайте статистику
   - Привлекайте подписчиков

2. **Премиум канал** (платный):
   - Более детальные сигналы
   - Ранний доступ (за 1-2 минуты до бесплатного)
   - Эксклюзивные стратегии
   - Поддержка в чате

3. **VIP группа**:
   - Прямое копирование через API
   - Персональные настройки
   - 1-на-1 консультации

### Настройка многоуровневого доступа

```bash
# Public channel (бесплатный)
TG_PUBLIC_CHANNEL=@my_signals_free
TG_PUBLIC_SIGNAL_DELAY=300  # 5 минут задержки

# Premium channel (платный)
TG_PREMIUM_CHANNEL=@my_signals_premium
TG_PREMIUM_SIGNAL_DELAY=0  # Без задержки

# VIP API (прямое копирование)
TG_VIP_API_ENABLED=true
TG_VIP_API_USERS=user1,user2,user3
```

---

## 🔐 Безопасность Leader Mode

### Защита от копирования front-running

**Проблема:** Быстрые боты могут видеть ваш сигнал и входить раньше вас, двигая цену.

**Решение:**

1. **Входите ДО публикации:**
   ```bash
   TG_SIGNAL_PUBLISH_DELAY=30  # Публиковать через 30 сек после входа
   ```

2. **Используйте limit ордера:**
   ```bash
   # Публикуйте цену входа, но входите сами по лучшей
   ```

3. **Разделяйте вход:**
   ```bash
   # 50% сразу, 50% после публикации
   ```

### Защита от подделки

Добавьте подпись к сигналам:

```
🟢 LONG BTCUSDT
...
✅ Verified Signal
Bot: @my_trading_bot
Time: 2025-11-16 12:34:56 UTC
Hash: a7f3d9e2...
```

---

## 🧪 Тестирование системы

### 1. Тест Leader Mode

```bash
# Запустите бота на testnet
TESTNET=true python start_bot.py

# Проверьте:
# ✅ Сигналы публикуются в канал
# ✅ Формат сигнала правильный
# ✅ Все данные присутствуют (entry, TP, SL)
# ✅ Обновления публикуются
# ✅ Закрытия публикуются с результатом
```

### 2. Тест Follower Mode

```bash
# Метод 1: Ручной тест
# 1. Опубликуйте тестовый сигнал в канале вручную
# 2. Проверьте, что фолловер-бот его распознал
# 3. Убедитесь, что бот открыл позицию

# Метод 2: Автоматический тест
python tests/test_copy_trading.py

# Проверьте:
# ✅ Бот читает сигналы из канала
# ✅ Парсинг сигнала корректный
# ✅ Позиция открывается с правильными параметрами
# ✅ TP/SL устанавливаются
# ✅ Риск-менеджмент работает
# ✅ Закрытие синхронизируется
```

---

## 🐛 Troubleshooting

### Leader Mode проблемы

**Проблема:** Сигналы не публикуются

```bash
# Проверьте:
✅ Бот добавлен в канал как админ?
✅ Права на публикацию сообщений?
✅ TG_TRADE_NOTIFICATIONS=true ?
✅ Правильный TG_CHAT_ID?

# Тест:
python -c "
from infra.telegram_bot import TelegramDashboardBot
import asyncio
import os
bot = TelegramDashboardBot(os.getenv('TG_BOT_TOKEN'), os.getenv('TG_CHAT_ID'))
asyncio.run(bot.send_message('Test'))
"
```

**Проблема:** Сигналы публикуются с задержкой

```bash
# Telegram rate limits:
# - 30 сообщений в секунду (в группы)
# - 20 сообщений в секунду (в каналы)

# Решение:
TG_RATE_LIMIT_MESSAGES=20
TG_RATE_LIMIT_PERIOD=1.0
```

### Follower Mode проблемы

**Проблема:** Бот не видит сигналы

```bash
# Убедитесь что:
✅ Бот добавлен в канал
✅ У канала privacy settings позволяют ботам читать
✅ Используете правильный метод (polling vs webhooks)

# Для приватных каналов:
# Бот должен быть админом или канал должен быть public
```

**Проблема:** Сигналы парсятся неправильно

```bash
# Debug режим:
COPY_DEBUG_MODE=true
COPY_LOG_ALL_MESSAGES=true

# Проверьте формат:
# Ваш парсер должен соответствовать формату источника
```

**Проблема:** Большое проскальзывание

```bash
# Причины:
# 1. Медленная сеть
# 2. Популярный источник (все копируют сразу)
# 3. Низкая ликвидность

# Решения:
COPY_EXECUTION_DELAY_MS=0  # Быстрее входить
COPY_USE_MARKET_ORDERS=true  # Вместо limit
COPY_MAX_PRICE_DEVIATION_PCT=1.0  # Больший допуск

# Или:
COPY_SKIP_ON_HIGH_SLIPPAGE=true  # Пропускать
```

---

## 📈 Анализ производительности

### Leader Mode метрики

Публикуйте прозрачную статистику:

```
📊 MONTHLY PERFORMANCE

Total Trades: 245
Win Rate: 64.5% (158W / 87L)
Profit Factor: 2.1x

ROI: +38.7%
Max Drawdown: -8.2%
Sharpe Ratio: 1.85

Best Trade: +$145.50
Worst Trade: -$42.30

Average Win: +$18.50 (+3.2%)
Average Loss: -$9.20 (-1.8%)

Total Followers: 1,247
```

### Follower Mode метрики

Отслеживайте:

```python
# Сравнение с источником
original_pnl = 1000.0  # P&L источника
copied_pnl = 850.0     # Ваш P&L

slippage = (1 - copied_pnl / original_pnl) * 100
# = 15% slippage (потери на копировании)

# Причины проскальзывания:
# - Задержка входа/выхода
# - Комиссии
# - Спред
# - Ликвидность
```

---

## 💡 Советы и лучшие практики

### Для Leaders (публикация сигналов)

1. **Будьте прозрачны:**
   - Публикуйте ВСЕ сделки (не только победные)
   - Показывайте реальную статистику
   - Объясняйте логику входов

2. **Документируйте стратегию:**
   - Таймфрейм
   - Индикаторы
   - Риск на сделку
   - Максимальное плечо

3. **Обучайте подписчиков:**
   - Объясняйте почему вошли
   - Показывайте технический анализ
   - Делитесь ошибками

### Для Followers (копирование)

1. **Due Diligence:**
   - Проверьте историю (минимум 3 месяца)
   - Посмотрите drawdown
   - Проверьте отзывы
   - Начните с demo/testnet

2. **Диверсификация:**
   - Не копируйте только один источник
   - Разделите капитал между 3-5 источниками
   - Используйте разные стратегии

3. **Мониторинг:**
   - Проверяйте производительность еженедельно
   - Сравнивайте с источником
   - Будьте готовы отключиться

---

## 🔗 Полезные ресурсы

### Telegram боты для копи-трейдинга

- [@userinfobot](https://t.me/userinfobot) - получить Chat ID
- [@getidsbot](https://t.me/getidsbot) - альтернатива для Chat ID
- [@BotFather](https://t.me/botfather) - создать бота

### Инструменты мониторинга

- **TradingView** - технический анализ
- **CoinGlass** - ликвидации, funding rates
- **Binance** - официальный обменник

### Сообщества

- [Crypto Trading Strategies](https://t.me/cryptotradingstrategies)
- [Binance Futures Signals](https://t.me/binancefuturessignals)

---

## ✅ Checklist запуска

### Leader Mode
- [ ] Создал Telegram канал
- [ ] Добавил бота как админ
- [ ] Настроил TG_CHAT_ID в .env
- [ ] Включил TG_TRADE_NOTIFICATIONS=true
- [ ] Выбрал формат сигнала
- [ ] Протестировал на testnet
- [ ] Опубликовал описание стратегии
- [ ] Запустил бота на mainnet
- [ ] Проверил первые сигналы
- [ ] Настроил ежедневную статистику

### Follower Mode
- [ ] Нашел проверенный источник сигналов
- [ ] Создал бота-читателя
- [ ] Получил доступ к каналу источника
- [ ] Настроил .env.follower
- [ ] Установил риск-лимиты
- [ ] Настроил фильтры копирования
- [ ] Протестировал на testnet
- [ ] Проверил парсинг сигналов
- [ ] Проверил проскальзывание
- [ ] Запустил на mainnet с минимальным размером
- [ ] Настроил мониторинг производительности
- [ ] Сравнил результаты с источником

---

## 🎓 Дополнительное обучение

### Создание кастомного парсера

Если формат сигналов источника не стандартный, создайте свой парсер:

```python
# runner/signal_parser.py

import re
from typing import Optional, Dict

class SignalParser:
    """Парсер торговых сигналов из Telegram."""

    @staticmethod
    def parse_signal(message: str) -> Optional[Dict]:
        """
        Парсит сигнал из текста сообщения.

        Поддерживаемые форматы:
        1. "LONG BTCUSDT Entry: 41250 TP: 42780 SL: 40435"
        2. "🟢 LONG BTCUSDT\nEntry: 41250\nTP: 42780\nSL: 40435"
        """

        # Detect side
        side = None
        if 'LONG' in message.upper():
            side = 'LONG'
        elif 'SHORT' in message.upper():
            side = 'SHORT'

        if not side:
            return None

        # Extract symbol
        symbol_match = re.search(r'(BTC|ETH|BNB|SOL|ADA|XRP|DOGE|AVAX|LINK|APT)USDT', message.upper())
        if not symbol_match:
            return None
        symbol = symbol_match.group(0)

        # Extract prices
        entry_match = re.search(r'Entry:?\s*\$?(\d+\.?\d*)', message, re.IGNORECASE)
        tp_match = re.search(r'TP:?\s*\$?(\d+\.?\d*)', message, re.IGNORECASE)
        sl_match = re.search(r'SL:?\s*\$?(\d+\.?\d*)', message, re.IGNORECASE)

        if not entry_match:
            return None

        signal = {
            'side': side,
            'symbol': symbol,
            'entry_price': float(entry_match.group(1)),
            'take_profit': float(tp_match.group(1)) if tp_match else None,
            'stop_loss': float(sl_match.group(1)) if sl_match else None,
        }

        # Extract leverage (optional)
        lev_match = re.search(r'Lev(?:erage)?:?\s*(\d+)x?', message, re.IGNORECASE)
        if lev_match:
            signal['leverage'] = int(lev_match.group(1))

        return signal

# Пример использования:
# parser = SignalParser()
# signal = parser.parse_signal(telegram_message)
# if signal:
#     await execute_copy_trade(signal)
```

---

**🎉 Готово! Теперь вы можете публиковать или копировать торговые сигналы!**

**⚠️ ВАЖНО:** Всегда тестируйте на testnet перед использованием реальных средств!

**💬 Вопросы?** Проверьте [TELEGRAM_SETUP.md](./TELEGRAM_SETUP.md) для базовой настройки Telegram бота.

---

*Последнее обновление: 2025-11-16*
*Версия: 1.0.0*
