# 🎯 IMBA + RL Position Advisor - Как это работает

## 📊 Концепция

**IMBA** и **RL Position Advisor** работают **ВМЕСТЕ** как единая система:

```
┌──────────────────────────────────────────────────────────────┐
│              ПОЛНЫЙ ЖИЗНЕННЫЙ ЦИКЛ ПОЗИЦИИ                   │
└──────────────────────────────────────────────────────────────┘

┌─────────────────┐
│  1. ОТКРЫТИЕ    │  ← IMBA Strategy
└─────────────────┘
         │
         ├─→ Генерирует сигнал BUY/SELL
         ├─→ Открывает позицию
         ├─→ Ставит TP1 (+5%), TP2 (+10%), TP3 (+20%)
         └─→ Ставит начальный stop-loss (-2%)

┌─────────────────┐
│  2. TP1 СРАБОТАЛ│  ← IMBA держит контроль
└─────────────────┘
         │
         └─→ Стоп остается на месте

┌─────────────────┐
│  3. TP2 СРАБОТАЛ│  ← RL Position Advisor АКТИВИРУЕТСЯ
└─────────────────┘
         │
         ├─→ Стоп переносится в безубыток (entry price)
         ├─→ RL включает trailing stop
         └─→ RL начинает мониторинг разворота

┌─────────────────┐
│  4. УПРАВЛЕНИЕ  │  ← RL Position Advisor
└─────────────────┘
         │
         ├─→ Trailing Stop: двигает стоп на -3% от максимума цены
         ├─→ Мониторинг: проверяет сигналы разворота каждую свечу
         │
         ├─→ Если разворот ≥75% уверенность → досрочное закрытие
         ├─→ Если разворот ≥95% уверенность → ЭКСТРЕННОЕ закрытие
         │
         └─→ Если цена идет к TP3 → trailing stop защищает прибыль

┌─────────────────┐
│  5. ЗАКРЫТИЕ    │  ← RL или TP3
└─────────────────┘
         │
         ├─→ TP3 достигнут (+20% прибыль)
         ├─→ RL закрыл досрочно (защита от разворота)
         └─→ Trailing stop сработал (зафиксирована прибыль)
```

---

## 🚀 Запуск

### Простой способ (рекомендуем):

```bash
./start_with_rl.sh
```

Выберите режим:
- `[1]` Testnet (безопасное тестирование)
- `[2]` Mainnet (реальные деньги)

### Прямая команда:

```bash
# Testnet
python cli.py live --timeframe 30m --testnet --use-combo --verbose

# Mainnet
python cli.py live --timeframe 30m --use-combo --verbose
```

**ВАЖНО:** Флаг `--use-combo` автоматически включает IMBA!

---

## ⚙️ Настройки RL Position Advisor

В `core/config.py` или через переменные окружения:

```python
# Минимальная уверенность для досрочного закрытия
rl_close_confidence_min = 0.75  # 75%

# Экстренное закрытие при сильном сигнале разворота
rl_emergency_confidence = 0.95  # 95%

# Дистанция trailing stop от пика цены
rl_trailing_distance_pct = 3.0  # 3%
```

### Через .env:

```bash
export RL_CLOSE_CONFIDENCE_MIN=0.75
export RL_EMERGENCY_CONFIDENCE=0.95
export RL_TRAILING_DISTANCE_PCT=3.0
```

---

## 📈 Пример работы

### Сценарий: LONG BTC @ $60,000

**Открытие (IMBA):**
- Entry: $60,000
- TP1: $63,000 (+5%)
- TP2: $66,000 (+10%)
- TP3: $72,000 (+20%)
- Stop: $58,800 (-2%)

**TP1 сработал @ $63,000:**
- Частично закрыто (40% позиции)
- Стоп остается $58,800

**TP2 сработал @ $66,000:**
- Еще 35% позиции закрыто
- Стоп → $60,000 (безубыток)
- 🤖 **RL ADVISOR АКТИВИРОВАН**

**Цена растет → $68,400 (+14%):**
- Trailing stop → $66,348 ($68,400 - 3%)
- Прибыль защищена: минимум +10.58%

**RL обнаруживает разворот (80% уверенность):**
- RL закрывает досрочно @ $68,200
- Итоговая прибыль: +13.67%
- **Избежали падения к $60,000** (безубыток)

### Без RL Advisor:
- Цена упала бы к безубытку $60,000
- Итоговая прибыль: +0%
- **Упущено +13.67%!**

---

## 🎯 Режимы работы

| Флаги | IMBA | RL Advisor | Описание |
|-------|------|------------|----------|
| `--use-combo` | ✅ (авто) | ✅ | Полная система (рекомендуем) |
| `--use-imba` | ✅ | ❌ | Только IMBA без RL |
| Без флагов | ✅ | ❌ | IMBA по умолчанию |

**Рекомендуем:** Используйте `--use-combo` для максимальной защиты прибыли!

---

## ❓ FAQ

### Нужно ли обучать COMBO модели?

**Да**, если хотите использовать RL Position Advisor. Запустите:

```bash
python run_full_combo_system_multi.py --symbols BTCUSDT,ETHUSDT,BNBUSDT --quick
```

### Можно ли использовать только IMBA?

Да, просто запустите с `--use-imba` или вообще без флагов (IMBA включена по умолчанию).

### COMBO открывает позиции?

**Нет!** COMBO используется только для:
1. Обнаружения разворотов (для досрочного закрытия)
2. Управления trailing stop

**Позиции открывает IMBA.**

### Как узнать, что RL работает?

Ищите в логах:
- `🤖 [RL_ADVISOR] Position registered`
- `🤖 [RL_TRAIL] Trailing stop updated`
- `🤖 [RL_CLOSE] RL Agent recommends CLOSE`

---

## 📊 Лучшие настройки

**Консервативный режим (меньше рисков):**
```python
rl_close_confidence_min = 0.80    # Закрывать только при 80%+ уверенности
rl_trailing_distance_pct = 2.0    # Ближе к цене (2%)
```

**Агрессивный режим (больше прибыли):**
```python
rl_close_confidence_min = 0.70    # Закрывать при 70%+ уверенности
rl_trailing_distance_pct = 5.0    # Дальше от цены (5%)
```

**Сбалансированный (по умолчанию):**
```python
rl_close_confidence_min = 0.75    # 75%
rl_trailing_distance_pct = 3.0    # 3%
```

---

## 🚀 Quick Start

```bash
# 1. Обучить модели (если еще не обучены)
python run_full_combo_system_multi.py --symbols BTCUSDT,ETHUSDT,BNBUSDT --quick

# 2. Запустить с RL Position Advisor
./start_with_rl.sh

# 3. Выбрать Testnet [1]

# 4. Наблюдать за логами:
#    - IMBA открывает позиции
#    - RL управляет после TP2
#    - Trailing stop защищает прибыль
```

**Готово!** 🎉

---

## 💡 Преимущества IMBA + RL

✅ **Защита прибыли** - trailing stop после TP2
✅ **Предотвращение откатов** - досрочное закрытие при развороте
✅ **Автоматизация** - не нужно вручную двигать стопы
✅ **Умное управление** - RL анализирует 22 параметра рынка
✅ **Проверенная стратегия** - IMBA + машинное обучение

---

**Удачной торговли!** 🚀📈
